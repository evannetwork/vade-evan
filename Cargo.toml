[package]
name = "vade-evan"
version = "0.1.1"
authors = ["evan GmbH"]
edition = "2018"
license-file = "LICENSE.txt"
description = "zero-knowledge-proof VC and TnT DID handling for vade"
homepage = "https://github.com/evannetwork/vade-evan"
documentation = "https://docs.rs/crate/vade-evan"
repository = "https://github.com/evannetwork/vade-evan"
readme = "README.md"
keywords = ["did", "ssi", "vc", "zkp"]

[lib]
name = "vade_evan"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "vade_evan_cli"
path = "src/main.rs"
required-features = ["cli"]

[package.metadata.wasm-pack.profile.release]
wasm-opt = ["-Oz", "--enable-mutable-globals"]

[features]
default = ["cli", "did", "didcomm", "portable", "vc-zkp"]

# add command line interface; not compatible with `wasm` feature
cli = [
    "clap",
    "did",
    "didcomm",
    "tokio",
    "vc-zkp",
]

# expose C interface for C applications to use vade
c-lib = [
    "did",
    "didcomm",
    "tokio",
    "vc-zkp",
]

# enables using DIDs, also required for some vc-zkp functions
did = [
    "did-substrate",
    "did-universal-resolver",
    "did-read",
    "did-write",
]

# enables zkp plugins, required for zkp plugins
vc-zkp = [
    "vc-zkp-bbs",
    "vc-zkp-cl",
]

# dedicated vc handlers
vc-zkp-bbs = [ "vade-evan-bbs" ]
vc-zkp-cl = [ "vade-evan-cl" ]

# enables did_resolve related functions and commands
did-read = []

# enables did_create, did_update related functions and commands
did-write = []

# dedicated did resolvers
did-sidetree = [ "vade-sidetree" ]
did-substrate = [ "vade-evan-substrate" ]
did-universal-resolver = [ "vade-universal-resolver" ]

# enables didcomm message features
didcomm = [
    "vade-didcomm",
]

# default native setup
portable = [
    "vade-didcomm/portable",
    "vade-evan-cl/portable",
]

# enables JWT signature based VC support
vc-jwt = [
   "vade-jwt-vc",
]

# build to run as wasm file; not compatible with `native` feature
wasm = [
    "vade-didcomm/wasm",
    "vade-evan-cl/wasm",
    "vade-evan-bbs/wasm",
]

[dependencies]
async-trait = "0.1.31"
cfg-if = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0.53", features = ["preserve_order", "raw_value"] }
vade = { git = "https://git.slock.it/equs/interop/vade/vade.git", branch = "develop", version = "0.1.0" }
###################################################################### feature specific dependencies
# feature "vc-zkp-bbs"
vade-evan-bbs = { git = "https://git.slock.it/equs/interop/vade/vade-evan-bbs.git", branch = "develop", version = "0.1.1", optional = true, default-features = false }
# feature "vc-zkp-cl"
vade-evan-cl = { git = "https://git.slock.it/equs/interop/vade/vade-evan-cl.git", branch = "develop", version = "0.1.1", optional = true, default-features = false }
# feature "cli"
clap = { version = "2.33.1", optional = true }
tokio = { version = "=1.7.1", optional = true, features = ["rt-multi-thread", "macros"] }
# feature "did-substrate"
vade-evan-substrate = { git = "https://git.slock.it/equs/interop/vade/vade-evan-substrate.git", branch = "develop", version = "0.1.1", optional = true }
# feature "vc-jwt"
vade-jwt-vc = { git = "https://git.slock.it/equs/interop/vade/vade-jwt-vc.git", branch = "develop", version = "0.0.1", optional = true }
# feature "did-universal-resolver"
vade-universal-resolver = { git = "https://git.slock.it/equs/interop/vade/vade-universal-resolver.git", branch = "develop", version = "0.0.3", optional = true }
# feature "did-sidetree"
vade-sidetree = { git = "https://git.slock.it/equs/interop/vade/vade-sidetree.git", branch = "develop", version = "0.0.1", optional = true }
# feature "didcomm"
vade-didcomm = { git = "https://git.slock.it/equs/interop/vade/vade-didcomm.git", branch = "develop", version = "0.0.2", default-features = false, optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
console_error_panic_hook = "0.1.6"
console_log = { version = "0.2", features = ["color"] }
log = "0.4.8"
serde_derive = "1.0.114"
wasm-bindgen = { version = "0.2",features = ["serde-serialize"] }
wasm-bindgen-futures = "0.4.5"

[dev-dependencies]
tokio = { version = "0.2.22", features = ["macros", "rt-threaded"] }

[target.'cfg(target_arch = "wasm32")'.dev-dependencies]
wasm-bindgen-test = "0.3.12"
